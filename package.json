{
  "name": "showcase-2",
  "version": "1.0.0",
  "description": "Showcases (sites) for Partner links platform.",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "cls && pnpm exec biome lint --write .",
    "format": "cls && pnpm exec biome format --write .",
    "clean": "turbo run clean",
    "upgrade-packages": "pnpm update -r --latest",
    "upgrade-packages:check": "pnpm outdated -r"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "@types/node": "^24.5.2",
    "tsx": "^4.20.5",
    "turbo": "^2.5.7",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.17.0"
}
